#include "main.h"
/**
 * mod - modulo of two integers
 *
 * Description: to modulo two integers
 * @a: the first integer
 * @b: the second integer
 *
 * Return: (0)
 *
 */
int mod(int a, int b)
{
	int result = a % b;

	return (result);
}
