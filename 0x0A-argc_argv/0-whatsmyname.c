#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
  * main - prints its name
  * @argc: the size of array argv
  * @argv: the array contains the size of argc
  * Return: 0
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
