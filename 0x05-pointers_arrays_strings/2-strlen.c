#include "main.h"
#include <stdio.h>
#include <string.h>
/**
  * _strlen - returns the length of a string
  * @s: the length of string to be return
  * Return: the length of a string
  */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
